type Query {
  products: [Product]
  productsByPrice(min: Float!, max: Float!): [Product]
  product(id: ID!): Product
}

type Product {
  id: ID!
  price: Int!
  description: String
  reviews: [Review]
}

type Review {
  rating: Float!
  comment: String
}
